version: "3"
   
services:
  redis:
    image: "redis:alpine"
  jerry:
    build: .
    command: python3 backend/manage.py runserver 0.0.0.0:8000
    volumes:
      - ./:/code
    ports:
      - "8000:8000"
    depends_on:
      - redis